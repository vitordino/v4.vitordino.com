---
import { cn } from 'src/utils/cn'
export type Props = { class?: string; as?: string }
const className = Astro.props.class
const Element = Astro.props.as || 'div'
---

<Element class={cn('mx-auto max-w-7xl px-4 md:px-8 lg:px-10', className)}>
	<slot />
</Element>
